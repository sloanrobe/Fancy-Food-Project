export class Supplier {
    supplier: string;
    quality_score: string;

    constructor(supplier: string, quality_score: string) {
        this.supplier = supplier,
        this.quality_score = quality_score
    }
}